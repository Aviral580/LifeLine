node_modules
./node_modules

Based on the code you have implemented and connected, here is the exact user flow that happens when a person visits your website right now. This flow proves that your Analytics, NLP, and UI systems are working as one unit.

Phase 1: Arrival & UI Rendering
Entry: The user opens the website. The ModeProvider (which you built) initializes.

Environment: The system checks if an emergency is active. Since it's not, the UI renders the Normal Mode (Blue/Light theme).

Analytics Start: A unique sessionId is generated for this user.

Phase 2: The "Smart" Search (Your NLP Task)
Typing: The user starts typing "earth" in the search bar.

Trigger: On every keystroke, your frontend SearchInterface sends a request to: GET /api/search/predict?q=earth.

NLP Brain: The Backend N-gram Engine searches its Trie memory. It finds "earthquake safety tips" (from your seeder).

Prediction: The user instantly sees the suggestion dropdown. They click it. This is your Next-Word Prediction in action.

Phase 3: Search Execution & Logging
The Search: The user hits the "Arrow" button.

Analytics Log: Your frontend sends a POST request to /api/search/log.

Backend Action: * Memory: The N-gram engine "learns" that "earthquake safety tips" was searched again, increasing its frequency in the QueryCorpus.

Tracking: A record is saved in AnalyticsLog (e.g., Action: Search, Query: earthquake safety tips, Mode: Normal).

Phase 4: Results & Mode Toggle (UI/UX Task)
Display: The user sees the search result cards. Even though the news is currently static, your Trust Score Indicators (the Green/Red badges) are visible.

Emergency Switch: The user sees the red Emergency Toggle. They click it.

UI Transformation: The entire website instantly turns Dark/Red. The isEmergencyMode state changes to true globally. The user now feels the "Urgency" you designed.

Phase 5: The Feedback Loop (Analytics Task)
The Click: The user clicks on the "National Disaster Management Authority" result.

CTR Logging: Your frontend sends another log to the backend: actionType: click_result.

Feedback: The user clicks the "Helpful" (Upvote) button on a card.

Database Update: The Feedback model in MongoDB saves an entry with trustScoreImpact: 1.

Phase 6: The "Closing the Loop" (Dashboard Task)
Live Monitoring: You (the admin/Abhay) look at the Analytics Dashboard on the right side of the screen.

Real-Time Update: Every 5 seconds, the dashboard fetches data.

Visualization: The CTR Bar Chart suddenly grows! It now shows that the "NDMA" link has 1 new click.

Proof of Value: You have just tracked a user’s journey from a "thought" (search) to a "trusted action" (click).

Summary of what you have achieved:
The UI responds to the user (Mode switching).

The AI predicts what the user wants (N-grams).

The Database remembers everything the user did (Analytics).

The Dashboard proves the system is working (Recharts).
